<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Post</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <%- include('../partials/navbar') %>

    <div class="container">
        <h1>Edit Post</h1>

        <form action="/posts/<%= post._id %>/edit" method="POST" enctype="multipart/form-data">
            <div>
                <label for="country">Country:</label>
                <input type="text" id="country" name="country" value="<%= post.country %>" required>
            </div>
            <div>
                <label for="city">City:</label>
                <input type="text" id="city" name="city" value="<%= post.city %>" required>
            </div>
            <div>
                <label for="startDate">Start Date:</label>
                <input type="date" id="startDate" name="startDate" value="<%= post.startDate.toISOString().split('T')[0] %>" required>
            </div>
            <div>
                <label for="endDate">End Date:</label>
                <input type="date" id="endDate" name="endDate" value="<%= post.endDate.toISOString().split('T')[0] %>" required>
            </div>
            <div>
                <label for="writtenText">Description:</label>
                <textarea id="writtenText" name="writtenText" rows="5"><%= post.writtenText %></textarea>
            </div>
            <div>
                <label for="photos">Upload New Photos (Optional):</label>
                <input type="file" id="photos" name="photos" multiple accept="image/*">
            </div>
            <div>
                <label for="video">Upload New Video (Optional):</label>
                <input type="file" id="video" name="video" accept="video/mp4">
            </div>
            <button type="submit">Update Post</button>
        </form>
    </div>
</body>
</html>
